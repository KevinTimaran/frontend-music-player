/**
 * Componente PlayerControls
 * Controles del reproductor: play, pause, next, previous
 */
export function PlayerControls() {
  return (
    <div className="player-controls">
      <h2>Player Controls</h2>
      {/* Play, pause, skip buttons will be here */}
    </div>
  )
}
