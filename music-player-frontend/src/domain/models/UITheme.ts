/**
 * Modelo de tema de UI
 * Interfaz que define la estructura de un tema
 */
export interface UITheme {
  id: string
  name: string
  primaryColor: string
  secondaryColor: string
  backgroundColor: string
  textColor: string
  accentColor: string
}
